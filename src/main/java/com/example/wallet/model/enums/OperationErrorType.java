package com.example.wallet.model.enums;

public enum OperationErrorType {
    INVALID_JSON,
    MISSED_FIELDS,
    NEGATIVE_AMOUNT,
    CONSTRAINT_VIOLATION,
    UNKNOWN_ERROR,
    WALLET_NOT_EXIST
}
